import styled from 'styled-components'
import { colors } from '../../global'

export const OptionsContainer = styled.select`
    font-size: 1.5rem;
    padding: 0.2rem;
    border: none;
    border-radius: 0.2rem;
    background-color: ${colors.mainOrange};
`
